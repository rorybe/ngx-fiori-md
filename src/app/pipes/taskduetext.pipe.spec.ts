import { TaskDueTextPipe } from './taskduetext.pipe';

describe('TaskDueTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDueTextPipe();
    expect(pipe).toBeTruthy();
  });
});
