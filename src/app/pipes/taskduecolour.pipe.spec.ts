import { TaskduecolourPipe } from './taskduecolour.pipe';

describe('TaskduecolourPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskduecolourPipe();
    expect(pipe).toBeTruthy();
  });
});
