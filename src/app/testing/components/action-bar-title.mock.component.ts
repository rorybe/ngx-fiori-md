import { Component, Input } from '@angular/core';

@Component({
  selector: 'fd-action-bar-title',
  template: 'fundamentals action bar title component mock'
})
export class ActionBarTitleMockComponent {
}
