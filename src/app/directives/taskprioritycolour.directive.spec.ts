import { TaskprioritycolourDirective } from './taskprioritycolour.directive';

describe('TaskprioritycolourDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskprioritycolourDirective();
    expect(directive).toBeTruthy();
  });
});
