import { Component, Input } from '@angular/core';

@Component({
  selector: 'fd-action-bar-header',
  template: 'fundamentals action bar header component mock'
})
export class ActionBarHeaderMockComponent {
}
