import { Component, Input } from '@angular/core';

@Component({
  selector: 'fd-tile',
  template: 'fundamentals tile component mock'
})
export class TileMockComponent {
  @Input()
  isButton;x
}
