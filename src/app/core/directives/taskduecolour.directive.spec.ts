import { TaskduecolourDirective } from './taskduecolour.directive';

describe('TaskduecolourDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskduecolourDirective(null);
    expect(directive).toBeTruthy();
  });
});
