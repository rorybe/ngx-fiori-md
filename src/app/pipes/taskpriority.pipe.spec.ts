import { TaskpriorityPipe } from './taskpriority.pipe';

describe('TaskpriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskpriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
