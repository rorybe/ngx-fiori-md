import { Component, Input } from '@angular/core';

@Component({
  selector: 'fd-action-bar-actions',
  template: 'fundamentals action bar actions component mock'
})
export class ActionBarActionsMockComponent {
}
