import { Component, Input } from '@angular/core';

@Component({
  selector: 'fd-side-nav-list',
  template: 'fundamentals side nav list component mock'
})
export class SideNavListMockComponent {
}
